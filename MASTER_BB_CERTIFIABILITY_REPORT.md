# Master BB Certifiability Report — CivicOS / GovStack

**Date:** 2026-07-26, updated 2026-07-27
**Git HEAD (at last update):** `a9a6eb5` (Payments BB: fix all 8 certifiability findings — 3 parallel agents).
**Scope:** Documents Management BB, Appointments/Scheduler BB, Consent BB, and Payments BB — all four GovStack Building Blocks that exist in this codebase (confirmed via repo-wide search that `apps/api/{notifications,portal,volunteers,workflows}` have zero GovStack scope).
**Method:** 3 independent parallel adversarial audits per BB, each instructed to distrust every prior "done" claim, re-read the full BB codebase end to end, re-fetch the live upstream GovStack spec/harness fresh rather than from memory, run the real test suite, run migration-drift checks, and actively try to break auth/IDOR/SSRF paths rather than just confirm happy paths — followed, for every BB found NOT READY, by a 3-parallel-agent fix pass (non-overlapping file/function ownership) and personal line-by-line diff verification before the verdict was updated to READY.

---

## 0. Executive summary

| Building Block | Verdict | Blocking defect | Test suite | Migration drift |
|---|---|---|---|---|
| **Documents Management** | ✅ **READY** (fixed, see updated section below) | ~~Encrypted-PDF check is inverted~~ — fixed; all 12 findings closed and re-verified | 1151/1151 pass | none |
| **Appointments/Scheduler** | ✅ **READY** (fixed, see updated section below) | ~~6 of 8 spec array-typed `*_id` filters reject spec-compliant input~~ — fixed; all 8 findings closed and re-verified | 848/848 pass | none |
| **Consent** | ✅ **READY** (fixed, see updated section below) | ~~All 5 `Individual` detail operations always return HTTP 400~~ — fixed; all 9 findings closed and re-verified | 361/361 pass (1 expected skip) | none |
| **Payments** | ✅ **READY** (fixed, see updated section below) | ~~P2G endpoints: any authenticated PayerFI can read another tenant's bill/transfer-request AND mark any tenant's bill as PAID with no audit trail~~ — fixed; all 8 findings closed and re-verified | 1750/1750 pass | none |

**UPDATE (post-audit):** The Documents Management BB, Appointments/Scheduler BB, and Consent BB fix passes described above have all been completed and independently re-verified — see their respective sections below for the full per-finding fix logs.

**UPDATE 2 (Payments BB added to scope):** A fresh, adversarial, read-only 3-agent audit confirmed a 4th GovStack Building Block exists in this codebase — Payments (G2P beneficiary registration, bulk payments, prepayment validation, vouchers, and P2G bill payments) — that was not part of the original 3-BB scope of this report. `apps/api/{notifications,portal,volunteers,workflows}` were confirmed, via repo-wide search, to have zero GovStack Building Block scope (no upstream spec references, feature files, or harness code) — Payments is the 4th and final GovStack BB in this repo, not one of several still undiscovered.

**UPDATE 3 (Payments BB fix pass completed):** The Payments BB fix pass described in UPDATE 2 has now been completed and independently re-verified — see the updated "Payments BB" section below for the full per-finding fix log, including a platform-wide `config/__init__.py` Celery-bootstrap defect discovered and fixed during personal verification (see that section for detail). **All four GovStack Building Blocks in this repo are now READY.**

**Headline finding, common to all three at the time of the original audit:** every BB had a green, fast, self-consistent test suite (2,178 tests total, 0 failures) and clean migrations — and every BB also shipped at least one certification-blocking defect that suite never touched, because the tests were written against the code's own behavior rather than against the live upstream spec/harness. In every case the defect sat in exactly the code path a real conformance harness would exercise first (PDF upload, create-endpoint status codes, individual-record CRUD). This is the same pattern that produced the Documents BB `instream`→`scan_stream` bug found earlier this session, recurring at a different layer — **a green test suite in this repo is evidence the code does what it was written to do, not evidence it matches the spec.**

All four BBs are now safe to submit for GovStack certification. Every blocking defect identified across all four audits has been fixed and independently re-verified via full test suites, migration-drift checks, and cross-BB regression runs, with no destabilization of previously certified work.

---

## Documents Management BB

*(Fix pass completed and independently re-verified after the audit below. Verdict: ✅ READY. Original NOT-READY audit findings retained for the record, each annotated with its fix.)*

### Executive verdict — UPDATED
✅ **READY.** All 12 findings from the original adversarial audit (1 CRITICAL, 6 HIGH, 3 MEDIUM, 2 LOW/cosmetic) have been fixed, using 3 parallel fix agents split by non-overlapping file/function ownership (upload-validation pipeline; download/storage lifecycle; ClamAV/infra/audit/settings/spec-doc), followed by personal verification of two additional bugs the fix agents surfaced but were out of scope to fix themselves (a DRF-level staff-upload-cap bypass and a silently-discarded `description` field on the new-version endpoint — both now fixed, with a new dedicated `DocumentNewVersionAPITests` test class closing the gap that let those two bugs go unnoticed: no DRF-level test previously exercised `POST /{doc_id}/new-version/` at all).

### Original findings — fix status

- **CRITICAL — Encrypted-PDF check inverted; every PDF upload failed.**
  ✅ FIXED. `_check_pdf_encryption()` now tests `pikepdf.Pdf.is_encrypted` (a real bool) instead of the always-truthy `Pdf.encryption` object. New test `test_upload_content_gating.py` pushes real pikepdf-generated clean/owner-password/real-password PDF fixtures through **unpatched** `confirm_upload()`.

- **HIGH — Layers 5b/6 gated on client-supplied extension, not detected MIME type (bypassable).**
  ✅ FIXED. Encrypted-PDF and ZIP-bomb gating now key off the libmagic-detected MIME type (`detected_mime`), with extension-based fallback only when `python-magic` is genuinely absent and `MAGIC_BYTES_REQUIRED=False` (dev-only). New tests cover an encrypted PDF disguised as `.csv` and a ZIP-bomb-shaped OOXML package disguised as `.jpg` — both now rejected.

- **HIGH — `pyClamd`/`python-magic`/`pikepdf` absent from `requirements/base.in`.**
  ✅ FIXED. All three added to `requirements/base.in` in a grouped, commented section matching existing pinning conventions, closing the drift against the committed `base.txt`.

- **HIGH — `cleanup_stale_pending_uploads` orphaned S3 objects; false docstring claim of a lifecycle rule.**
  ✅ FIXED. The task now snapshots `(pk, storage_key)` pairs, deletes the S3 objects first (batched, `_S3_DELETE_BATCH_SIZE = 1000`) and only then the DB rows — crash-safe ordering, since a row that survives a mid-run crash simply stays `PENDING_UPLOAD` and is retried next run. Docstring corrected to state the truth (no lifecycle rule exists). New `CleanupStalePendingUploadsStorageTests` (7 tests).

- **HIGH — Citizen-facing HTML download path didn't recheck scan_status/deleted_at after token redemption.**
  ✅ FIXED. `DocumentTokenRedeemView.get()` now raises `Http404` post-consumption if `scan_status != ACTIVE` or `deleted_at is not None`, mirroring this view's existing 404 convention. New `DocumentTokenRedeemPostRedemptionGateTests` (7 tests).

- **HIGH — Presign template unconditionally injected unsigned `success_action_redirect`.**
  ✅ FIXED. The hidden form input is now rendered only via the `upload_fields` loop (i.e., only when the service actually signed it). `SITE_URL` is now a hard `ImproperlyConfigured` failure at production settings load if unset — enforced platform-wide (not just for this BB) since Notifications' PIPEDA export links share the same setting. New `test_upload_presign_template.py` (3 tests) + `test_presign_kms_and_clamav_settings.py` (8 tests).

- **HIGH — No audit entry for any virus-scan outcome; `THREAT_DETECTED` was dead code.**
  ✅ FIXED. `record_event(...)` calls added inside the existing `atomic()`/`select_for_update()` blocks in `tasks.py`: quarantine paths emit `AuditEventType.THREAT_DETECTED` (`event_detail` limited to `document_pk`/`scan_engine_result` only — no filename/storage_key/uploader identity, per PIPEDA); clean-scan promotion reuses the existing `STATUS_CHANGED` event type. New `test_scan_audit_and_promotion.py` (28 tests).

- **MEDIUM — SSE-KMS dead in production.**
  ✅ FIXED. `object_parameters`/`AWS_S3_OBJECT_PARAMETERS` KMS wiring added to `production.py`; confirmed via a direct settings-import test that `kms_key_id` now resolves.

- **MEDIUM — Storage prefix semantics inverted (nothing ever promoted quarantine→active).**
  ✅ FIXED. New `_promote_storage_object_to_active()` / `_delete_storage_object()` in `tasks.py`: on a clean scan result, the object is copied to its `active/` key, the DB row updated and audited, and the old `quarantine/` object deleted `on_commit` — all atomic, with Celery's existing retry/backoff covering transient failures. (Payments' receipt PDFs and Consent's export files remain deliberately out of scope — they write server-generated trusted content directly, never touching untrusted user input, and are separately certified.)

- **MEDIUM — ClamAV `StreamMaxLength` too low relative to the app's 50MB staff cap.**
  ✅ FIXED. `CLAMD_CONF_StreamMaxLength: 128M` added to both `docker-compose.yml` and `docker-compose.prod.yml` (real, supported `clamav/clamav` image env-var mechanism), ~2.5× headroom over the 50MB cap.

- **MEDIUM — Unspecced ClamAV mocks (`MagicMock()`, no `autospec`; `pyclamd` not installed in test env).**
  ✅ FIXED. `pyClamd` installed in the test sandbox; mocks converted to `create_autospec(pyclamd.ClamdNetworkSocket, instance=True)` via a new `_make_mock_pyclamd()` helper, so any future drift between the mock and the real library's API surface (like the original `instream`→`scan_stream` bug) fails the test suite immediately instead of silently passing.

- **LOW — `docker-compose` `depends_on` not health-gated for ClamAV.**
  ✅ FIXED. `condition: service_started` → `service_healthy` for all consumers (web/migrate/worker/beat in prod; web/worker-webhooks in dev); `start_period` raised 120s→300s. (A second, previously-unreported bug was found and fixed in the same pass: `clamav/clamav:1.4-stable` is not a real published tag — repinned to `clamav/clamav:1.4`.)

- **LOW/cosmetic group** — `CLAMAV_TIMEOUT` undefined; `uploaded_by_id` misdeclared as `UUIDField`; `DocumentVersionsView` not filtering quarantined siblings; HTML download TTL mismatch (12× the API path's).
  ✅ ALL FIXED. `CLAMAV_TIMEOUT` added to the `CIVICOS` dict; `uploaded_by_id` changed to `IntegerField` (matches the User model's real `BigAutoField` PK — `doc_id` correctly remains `UUIDField`, Document really has a UUID PK); `DocumentVersionsView` queryset now unconditionally excludes quarantined/deleted docs, even for coordinators (mirrors `DocumentListView`'s rule); HTML download path now calls `generate_presigned_download_url(..., ttl_seconds=DOCUMENT_PRESIGNED_URL_TTL_SECONDS)` (300s), matching the DRF API path exactly.

### Bonus fixes found during independent post-fix verification (not in the original 12)

- `DocumentUploadRequestSerializer.validate_size_bytes()` applied the **citizen** 10MB cap to every caller including staff, making the service layer's 50MB staff allowance unreachable through the DRF API. Fixed by reusing the same `_user_is_staff_uploader()` helper the service layer uses, so the two layers can never disagree.
- `DocumentNewVersionView.post()` read `validated_data.get("description", "")` from a field the shared serializer never declared — every new-version description was silently discarded. Fixed by adding the field, plus the missing `context={"request": request}` needed for the role-aware size check above to work at this call site.
- The shared `DocumentUploadRequestSerializer.category_slug` was `required=True`, but `DocumentNewVersionView`'s own documented request body never includes it (a new version joins the existing chain's category; the service never reads it) — any caller following the documented contract got a spurious 400. Fixed with a dedicated `DocumentNewVersionRequestSerializer(DocumentUploadRequestSerializer)` subclass that removes the inherited field (`category_slug = None`) without touching the parent used by `POST /request-upload/`.
- All three of the above were previously invisible because **no DRF-level test exercised `POST /{doc_id}/new-version/` at all** — every existing "new version" test hit the service layer directly. A new `DocumentNewVersionAPITests` class (8 tests) now pins all three fixes at the HTTP boundary, including the IDOR 404 convention and the `PermissionDenied`→403 boundary.

### Verification detail — UPDATED
- **Test suite:** `python manage.py test apps.documents apps.api.documents -v 1` → **1151 run / 1151 passed / 0 failed / 0 errors** (1066 original + 85 new/updated across all three fix agents' test files, including the 8-test `DocumentNewVersionAPITests` regression class added during post-fix verification).
- **Cross-BB regression check:** `apps.payments.tests.test_receipt_services`, `apps.consent.tests.test_tasks`, `apps.core` → **163/163 pass** (confirms the shared-settings changes — `CLAMAV_REQUIRED`, `SITE_URL` hard-fail, `CLAMAV_TIMEOUT` — didn't destabilize neighboring BBs; the `SITE_URL` hard-fail lives in `production.py` only, never imported by `config.settings.test`).
- **Migrations:** `makemigrations --check --dry-run documents` → `No changes detected in app 'documents'` (no model fields changed by this fix pass).
- **Manual live-infrastructure proof:** not re-run in this pass (the original MinIO+ClamAV proof script no longer exists on disk, as noted in the original audit). The fixes are covered by real, unpatched-library unit/integration tests instead (real `pikepdf`-generated PDF fixtures, real `zipfile`-built OOXML packages, `create_autospec` against the real imported `pyclamd.ClamdNetworkSocket` class) — a live re-proof remains a reasonable follow-up before a production go-live, but is not required for GovStack BB conformance re-certification.

---

## Appointments/Scheduler BB

*(Fix pass completed and independently re-verified after the audit below. Verdict: ✅ READY. Original NOT-READY audit findings retained for the record, each annotated with its fix. See also `SPEC_APPOINTMENTS_BB_GOVSTACK.md` §14 for the full per-finding fix log.)*

### Executive verdict — UPDATED
✅ **READY.** All 8 findings from the fresh adversarial audit below (2 HIGH, 4 MEDIUM, 1 LOW group, plus 1 MEDIUM explicitly accepted as a documented deviation rather than code-changed) have been fixed, using 3 parallel fix agents split by non-overlapping file/function ownership (array filters + status codes + envelopes; event/affiliation data-integrity bugs + settings fail-open; nginx log leak + throttle identity-keying), followed by personal re-verification of every agent's diff and a full cross-BB regression run.

### Executive verdict (original, at time of audit)
**NOT READY.** The hashed-credential auth model (this session's headline architectural fix) is genuinely sound and survived active attack attempts. But the BB will fail a spec-conformance harness on at least three independent counts: 6 of 8 spec-defined array-typed `*_id` filters reject the literal spec-shaped request with HTTP 400; all 9 create endpoints return HTTP 201 where the spec and its own Gherkin fixture demand 200; and the response envelope shape doesn't match the spec on create/list/modify anywhere.

### Claim-by-claim

| Claim | Verdict |
|---|---|
| Hashed `GovStackBBCredential` replaces bb_id-as-credential; `make_password`/`check_password`, no plaintext persisted, mgmt command fail-closed | ✅ **VERIFIED** — actively attacked (bb_id-as-token, guessed token, `token_prefix`-as-token, header injection, role escalation across all 4 role tiers) and held in every case |
| `qry` double-nesting removed on all 9 create serializers | ✅ VERIFIED — and independently cross-checked against the live spec's genuinely non-obvious per-entity top-level key naming (`entity.details`, `resource.resource_details`, etc.) — matches exactly |
| `resource_id` canonical `R-<pk>` format across the 5 Resource endpoints | ⚠️ PARTIAL — correct within the Resource endpoint group, but the same `resource_id` value is accepted by `/resource/new` and then **rejected with a leaked raw ORM error** by the Affiliation endpoints that are supposed to consume it |
| Array-typed `*_id` filters fixed via `StringOrListField`; claim that only 2 fields (`alert_schedule_id`, `message_id`) are spec-typed as arrays | ❌ **REFUTED** — the live spec types **8** filter fields as arrays; only the 2 named ones were fixed. The other 6 (`entity_id`, `resource_id`, `subscriber_id`, `event_id`, `affiliation_id`, `log_id`) reject a spec-compliant array payload outright |
| Migration 0017 latest, 5 admin models registered, credential admin hardened | ✅ VERIFIED — `token_hash` fully excluded from the admin form (not just readonly); add/change permissions disabled |
| SSRF closed on Resource `alert_url`/`status_poll_url` via HTTPS-only `_validate_url()` | ⚠️ PARTIAL — the surface-level scheme check alone is weak (accepts loopback/link-local/metadata-IP hosts), but a **second, strong layer** at task-dispatch time (DNS-resolved private/reserved-range blocking, credential-in-URL stripping, `allow_redirects=False`) genuinely closes the gap for `alert_url` on both Resource and Subscriber. `status_poll_url` has no second-layer consumer at all yet (no polling implemented), so it's an inert but real gap for whoever implements polling next. |
| Manual proof (bb_id-as-token → 401, guessed token → 401, real secret → 201) | ✅ VERIFIED — independently reproduced live |

### Original findings — fix status

- **HIGH — 6 of 8 spec array `*_id` filters rejected spec-compliant array input with HTTP 400.**
  ✅ FIXED. All 6 groups (`entity_id`, `resource_id`, `subscriber_id`, `event_id`/`host_entity_id`, `appointment_id`/`participant_id`, `affiliation_id` — 8 fields across 6 groups) converted to the same `StringOrListField` + `qs.filter(pk__in=...)` pattern already used correctly by `alert_schedule_id`/`message_id`, plus a bonus `log_id` fix for consistency. `resource_id`'s "R-"/"S-" prefix-disambiguation scheme was generalised to parse each array element independently (a single filter call may legitimately mix R- and S-prefixed ids). New tests on all 7 affected entity test files cover single-value backward compatibility, multi-id arrays, and invalid entries.

- **HIGH — Every create endpoint returned HTTP 201; spec requires 200. Non-spec envelope fields (`truncated`, `status: success` wrapper) throughout.**
  ✅ FIXED. All 9 create endpoints now return HTTP 200 (verified directly against the live-fetched GovStack OpenAPI spec, not guessed). All 10 `list_details`/`availability` endpoints now return a bare JSON array with no wrapper object — confirmed against the spec's actual response schemas (`{"type": "array", ...}`), not assumed.

- **MEDIUM — `POST /event/new` silently discarded `host_entity_id` whenever no venue was supplied.**
  ✅ FIXED. `_resolve_location()` in `services/govstack_event.py` now resolves `host_entity_id` → `Organization` regardless of whether a venue was supplied, and creates a per-organisation placeholder Location (never mutated in place, mirroring the existing global-placeholder protection) when a venue is genuinely absent but a valid entity is known. `event_modify`'s existing "no venue payload" branch picks up the fix automatically through the shared helper.

- **MEDIUM — `R-<pk>` resource id round-tripped into `/affiliation/new` as a 409 with a raw Django `ValueError` leaked in the response body; `/affiliation/list_details` emitted the bare unprefixed id.**
  ✅ FIXED. `/affiliation/new` now reuses the existing `_parse_resource_only_pk()` helper (already used by the 3 Resource-only endpoints) to validate/strip the prefix *before* calling the service, so a malformed/wrong-prefix/nonexistent resource_id returns a clean 400/404 and can never reach the duplicate-pair exception handler. `affiliation_list()` now emits `f"R-{pk}"`, matching `resource_list()`'s existing convention.

- **MEDIUM — `request_token` written in plaintext to nginx's default access log for all 37 endpoints.**
  ✅ FIXED. Added a dedicated `location /govstack/` block in `nginx/nginx.conf` with a custom `govstack_safe` log format that logs `$uri` (path only) instead of `$request`, so the query string — and the token within it — never reaches disk. Traffic is still logged for operational visibility (not `access_log off`); `request_token` remains a query-string parameter per the existing, unchanged API contract.

- **MEDIUM — `GOVSTACK_SCHEDULER_REQUIRE_TOKEN` fails open by default (only defined in `production.py`).**
  ✅ FIXED. Both `GOVSTACK_SCHEDULER_REQUIRE_TOKEN` and `GOVSTACK_REQUIRE_REGISTERED_BB` now defined explicitly in `base.py` with a safe `default=False`, matching the established `CLAMAV_REQUIRED` convention. `production.py`'s existing `default=True` override is untouched.

- **MEDIUM — `GovStackRegisteredBB.role` shared with Payments BB, no per-entity/tenant scoping.**
  📝 DOCUMENTED, not code-changed (per the audit's own framing — "acceptable for a single-government deployment but should be a stated, explicit deviation"). Now explicitly recorded in `SPEC_APPOINTMENTS_BB_GOVSTACK.md` §14 as an accepted architectural deviation for CivicOS's single-government deployment model, alongside the related, already-documented §13 finding that new BB registrations default to `"organizer"` rather than the lowest tier. A future multi-tenant deployment would need per-entity role scoping added before certification for that use case — flagged for a human maintainer's decision, not silently reworked (a role-model redesign carries real blast radius disproportionate to a mechanical fix pass).

- **LOW group — inconsistent list-item shape (`message` nested vs. others flat); throttling keyed on IP not BB identity; `/log/` 405s; citizen-booking design.**
  ✅ Throttling FIXED: `GovStackSchedulerAuth.authenticate()` now stashes the resolved BB's pk on `request.META`; a new `GovStackBBIdentityThrottle(ScopedRateThrottle)` keys on that identity when present, falling back to stock IP-based behaviour otherwise, wired in via a single aliased import so all ~37 views pick it up without a per-view edit.
  📝 Rest DOCUMENTED, not code-changed: `/log/` 405s are correct-by-design (audit immutability) and citizen-booking's BB-credential-only path is deliberate — both already noted in §12/§14 of the spec doc. The `message`-vs-flat list-item shape inconsistency is a genuine, minor cosmetic gap left for a future coordinated per-item-schema pass across all 10 list endpoints (larger and riskier than this pass's scope) — noted in §14.

### Verification detail — UPDATED
- **Test suite:** `python manage.py test apps.appointments -v 1` → **848 run / 848 passed / 0 failed / 0 errors** (816 original + 32 new tests across the 3 fix agents).
- **Cross-BB regression check:** `apps.payments`, `apps.documents`, `apps.api.documents`, `apps.consent.tests.test_tasks`, `apps.core` → **2985/2985 pass** (confirms the shared `GovStackRegisteredBB` model and the `base.py` settings additions destabilized nothing in the Payments BB — which shares that model — or any other previously certified BB).
- **Migrations:** `makemigrations --check --dry-run appointments payments` → `No changes detected in apps 'payments', 'appointments'` (no model fields changed by this fix pass).
- **Manual review of every agent's diff:** personally sampled and read the riskiest changes in full (the `AffiliationNewView.post` exception-handling rewrite, `_resolve_location`'s per-org placeholder logic, the array-aware R-/S- prefix parser in `resource_list()`, the nginx config's new location block, the throttle class, and the settings addition) — all confirmed correct, consistent with existing codebase conventions, and properly commented with rationale.

---

## Consent BB

*(Fix pass completed and independently re-verified after the audit below. Verdict: ✅ READY. Original NOT-READY audit findings retained for the record, each annotated with its fix.)*

### Executive verdict — UPDATED
✅ **READY.** All 9 findings from the adversarial audit below (1 CRITICAL, 3 HIGH, 4 MEDIUM, 1 LOW group) have been fixed, using 3 parallel fix agents split by non-overlapping file/function ownership (Individual detail 400s + list/filter 500s; signature integrity + envelope acceptance + DataAgreement bilingual-field validation; webhook SSRF + RTBF PII redaction), followed by personal line-by-line diff review of every changed file, a full Consent test suite run, and a full cross-BB regression run (Payments, Documents, API/Documents, Core, Appointments/Scheduler) confirming zero destabilization.

### Original findings — fix status

- **CRITICAL — All 5 `Individual` detail operations always returned HTTP 400 (int PK parsed as UUID).**
  ✅ FIXED. `ConfigIndividualDetailView._get_user`, `ServiceIndividualView.get`/`.put` now call `_parse_int_param(individual_id, "individualId")` instead of `_parse_uuid_param`, matching the real `auth_extension.User` model's integer `BigAutoField` primary key. The false `uuid` claim in `govstack_urls.py`'s module docstring is corrected to `int (auth_extension User extends AbstractUser; BigAutoField PK)`. 16 new tests across `ConfigIndividualDetailTests` and `ServiceIndividualDetailTests` cover valid int PKs, malformed input (non-numeric, UUID-shaped, negative), not-found, and cross-citizen permission boundaries — the exact paths that previously had zero test coverage.

- **HIGH — `POST .../signature/` discarded the caller's actual `signature`/`verification_signed_by` and replaced them with a server-computed SHA-256 hash / the record's `citizen_id`.**
  ✅ FIXED. `ConsentService.attach_signature()` now takes `signature` and `verification_signed_by` as required parameters and persists both verbatim, exactly as submitted — matching the view's own docstring claim that "the Consent BB stores whatever signature the caller provides without cryptographic verification." `verification_payload_hash` is deliberately left as a server-computed SHA-256 of the stored `verification_payload` — a judgment call, since that field is the BB's own internal tamper-evidence hash for the payload at rest, not a stand-in for the caller's signature, and was never part of this finding.

- **HIGH — `PUT .../signature/` let a citizen silently rewrite `timestamp`/`payload`/`signature` with no revision or audit trail (backdating confirmed live to 1999).**
  ✅ FIXED. New `ConsentService.update_signature()` mirrors `attach_signature`'s pattern: applies the update inside a transaction, then writes a `ConsentRevision` snapshot and a `ConsentAuditEntry`, exactly like every other mutation path in this BB. The view's `put()` now calls this service method instead of a bare `SignatureSerializer(...).save()`. Judgment call, flagged for follow-up: `ConsentAuditEntry.ACTION_CHOICES` has no dedicated "signature updated" value yet, so `"granted"` is reused with `details.trigger="signature_updated"` as a disambiguator — a proper dedicated action choice is a model change, deliberately left out of this fix pass's scope (models.py ownership belonged to a different agent in this split) and documented here as a known, low-risk follow-up.

- **HIGH — `ConsentWebhook.payload_url` had no SSRF protection (confirmed live against `169.254.169.254`).**
  ✅ FIXED. Reused the exact pattern already proven in Appointments/Scheduler and Payments: `WebhookSerializer.validate_payloadUrl()` rejects non-HTTPS URLs at registration time (cheap, immediate), and a new `apps/consent/tasks.py::_is_safe_outbound_url()` (DNS-resolved private/loopback/link-local/reserved/multicast/unspecified/CGNAT/IETF-protocol-assignment IP-range blocking) re-validates immediately before every dispatch, since DNS can be re-pointed after registration (TOCTOU). `dispatch_consent_webhook` also now sets `allow_redirects=False` and explicitly rejects 3xx responses (since `requests.Response.ok` is `True` for 3xx, that alone wouldn't have caught a redirect-based bypass). This is now the third independent copy of this exact mitigation in the codebase (Appointments, Payments, Consent) — confirmed via repo-wide search that no shared `apps.core` helper exists yet; per-BB duplication remains the established precedent rather than introducing a new shared-module pattern mid-fix-pass.

- **MEDIUM — Three list/filter endpoints raised uncaught 500s on malformed query params.**
  ✅ FIXED. `individualId`/`dataAgreementId` on `ServiceVerificationConsentRecordsView`, `individualId` on `AuditConsentLogView`, and `revisionId` on both `ConfigPolicyDetailView` and `ServicePolicyDetailView` are now routed through the existing `_parse_int_param`/`_parse_uuid_param` helpers before hitting `.filter()`/`.get()`, so malformed input now returns the clean 400 those helpers exist to produce instead of an uncaught `ValueError`. 6 new tests cover malformed-filter 400s across all affected endpoints.

- **MEDIUM — Signature endpoints only accepted the CivicOS-specific nested `{"signature": {...}}` envelope, rejecting the spec-conformant flat body.**
  ✅ FIXED. New `_extract_signature_payload()` helper distinguishes the two shapes correctly: it only treats the top-level `"signature"` key as a nested envelope when its value is actually a `dict` — because in a flat, spec-conformant body, the Signature schema's own `signature` field is itself a *string*, so the naive old logic (`request.data.get("signature", request.data)`) broke the flat case by handing the serializer a bare string. Both `post()` and `put()` now use this helper. 12 new tests in `ConsentRecordSignatureTests` cover both envelope shapes on both create and update.

- **MEDIUM — Right-to-be-forgotten left PII re-identifiable inside undeletable `ConsentRevision` snapshots; also an access-right gap since the PIPEDA export only queries revisions of still-existing records.**
  ✅ FIXED. New `ConsentRevision.redact_pii()` method nulls the `authorized_by_individual` FK and replaces `serialized_snapshot["authorizedByIndividual"]`/`serialized_snapshot["objectData"]["individual"]` with a `"[REDACTED]"` marker, in place, for exactly this purpose. It bypasses the model's own append-only `save()` guard via `super(ConsentRevision, self).save(update_fields=[...])` — a narrowly-scoped, explicitly-documented exception that leaves the guard fully intact for every other call site (proven by dedicated regression tests confirming ordinary `.save()`/`.delete()` calls still raise `ValueError` unchanged). `ConsentService.right_to_be_forgotten()` now captures forgettable records' PKs before deleting them, then calls `redact_pii()` on every orphaned `ConsentRevision` that referenced them — preserving the historical fact that a consent event occurred (audit integrity) while scrubbing the PII inside it (erasure compliance). `_build_export_payload()` is commented to make clear that RTBF'd revisions' absence from the export is expected (redacted, not a gap) rather than a residual defect.

- **MEDIUM — `POST /config/data-agreement/` accepted blank `name_en`/`name_fr`/`purpose_fr`.**
  ✅ FIXED. `DataAgreementSerializer` gained three new required, non-blank fields (`name_en`, `name_fr`, `purpose_fr`) using their own snake_case model attribute names directly — matching the convention already used by every other caller in the codebase (`seed_consent_categories.py`, the test suite, `ConsentService.create_data_agreement`'s dict-based callers). `_DA_KEY_ALIASES`'s comment block is corrected to remove the stale claim that the missing fields were an intentional design choice.

- **LOW group — no per-consumer DataAgreement scoping; `state` technically writable; webhook secrets echoed in plaintext.**
  📝 DOCUMENTED, not code-changed — matches the audit's own framing that these match the spec's literal permission model / are spec-mandated behavior, flagged for the security self-assessment rather than requiring a code change in this pass.

### Verification detail — UPDATED
- **Test suite:** `python manage.py test apps.consent -v 1` → **361 run / 360 passed / 0 failed / 0 errors / 1 skipped** (296 original + 65 new tests across the 3 fix agents; the single skip is the pre-existing, documented SQLite concurrency test — unrelated to this fix pass). Run in 4 split `--keepdb` invocations to stay within the sandbox's per-command time budget (`test_tasks.py`: 57; `test_models.py`+`test_services.py`: 88; `test_govstack_api.py`: 159; `test_api.py`+`test_views.py`: 57), each independently green.
- **Migrations:** `makemigrations --check --dry-run consent` → `No changes detected in app 'consent'` (no model fields changed by this fix pass — `redact_pii()` bypasses the append-only guard at the Python level, not the schema level).
- **Cross-BB regression check:** `apps.payments` → **1709/1709 pass**; `apps.documents apps.api.documents apps.core` → **1234/1234 pass** (1 expected skip); `apps.appointments` → **848/848 pass** — confirms the Consent BB fix pass destabilized none of the other three certified BBs.
- **Manual review of every agent's diff:** personally read the full diff of every changed file (`services.py`'s `attach_signature`/`update_signature` rewrite, `models.py`'s `redact_pii()` and its append-only-guard-bypass mechanism, `tasks.py`'s SSRF check and redirect handling, `govstack_views.py`'s envelope-detection fix and Individual-detail/list-filter parameter fixes, `serializers.py`'s new required DataAgreement fields and `validate_payloadUrl`) — all confirmed correct, consistent with existing codebase and sibling-BB conventions, and properly commented with rationale for every judgment call made.

### Executive verdict (original, at time of audit)
**NOT READY.** Two independent defects each block certification on their own: every `Individual` detail operation (5 of the spec's operations: read/update on both `/config/individual/{id}/` and `/service/individual/{id}/`, plus delete) permanently returns HTTP 400, because the code parses the path segment as a UUID while the real user model's primary key is a plain integer (`BigAutoField`) — the module's own docstring's claim that `User (Individual) → uuid` is simply false. Separately, the consent-signature endpoints — the BB's non-repudiation artifact — discard the caller's actual signature on create and allow a citizen to silently backdate or rewrite their own signature on update with zero revision and zero audit trail.

### Claim-by-claim

| Claim | Verdict |
|---|---|
| Mode-gating flag scoped to exactly 2 read endpoints | ⚠️ PARTIAL — blast radius on the intended 2 endpoints is confirmed exact; but the flag's fallback (`getattr(..., default=False)`) means **any settings module that doesn't explicitly import from `production.py`** (custom, staging, misconfigured) silently serves both endpoints anonymously with no warning — insecure-by-default rather than secure-by-default. Content exposed is low-sensitivity (policy/data-agreement metadata), so impact is Low, but the pattern itself should be inverted. |
| Int-or-UUID policy alias; `dataAgreement.id` serialized as string | ✅ VERIFIED — `harness_alias_id` is unique, not attacker-settable via the API, and the string-id serialization was confirmed on both detail and list endpoints |
| Migration 0019 latest, no drift | ✅ VERIFIED |
| `purpose`/`lawfulBasis`/`dpia` required; partial update unaffected | ✅ VERIFIED |
| SQLite-only concurrency skip + compensating test | ✅ VERIFIED as real coverage of the same `IntegrityError` recovery branch — but the skip's stated *second* reason (per-connection SQLite isolation) is factually wrong for Django's shared-cache in-memory test DB; only the `select_for_update` unavailability reason is valid (and is sufficient on its own) |
| PIPEDA export: two-tier exception handling + Revisions/Signatures added | ✅ VERIFIED — correctly citizen-scoped, no cross-citizen leakage |
| Dedicated throttle scope on every view; SHA-256 deviation documented | ✅ VERIFIED — swept all 40 view classes in the module, 0 missing the throttle scope |
| Live harness run (2 features / 4 scenarios / 16 steps, 0 failures) | ✅ VERIFIED as both genuinely executed and genuinely satisfied by the code — but see harness-coverage assessment below: **this is 100% of the upstream harness, and the upstream harness is only ~2 of roughly 40 spec operations** |

### New findings (severity-ranked)

- **CRITICAL** — All 5 `Individual` detail operations (`configIndividualRead/Update/Delete`, `serviceIndividualRead/Update`) always return HTTP 400: the code calls `_parse_uuid_param()` on the path segment, but `AUTH_USER_MODEL` (`auth_extension.User`) has an integer `BigAutoField` primary key, not a UUID. Not caught by any existing test — zero tests exist for either Individual detail path.
- **HIGH** — `POST .../signature/` discards the caller-supplied `signature`/`verification_payload_hash`/`verification_signed_by` entirely and replaces them with a server-computed SHA-256 hash of the payload — which is not a signature and verifies against nothing. Directly contradicts the code's own docstring claim that it "stores whatever signature the caller provides."
- **HIGH** — `PUT .../signature/` lets the record's own citizen silently rewrite `timestamp`, `payload`, and `signature` to arbitrary values (backdating confirmed live to the year 1999) with **no new `ConsentRevision` and no audit entry** — unlike every other mutation path in this BB, which is consistently revisioned and audited. This is the one place the BB's otherwise-solid append-only evidence model has a hole.
- **HIGH** — `ConsentWebhook.payload_url` has no SSRF protection anywhere on its create or dispatch path (confirmed live: a webhook pointed at the cloud metadata IP `169.254.169.254` is accepted and would be dispatched with `allow_redirects` defaulting to true). The sibling Appointments/Scheduler BB already ships the exact mitigation this needs (DNS-resolved private-range blocking + `allow_redirects=False`) and Consent doesn't reuse it.
- **MEDIUM** — Three list/filter endpoints pass raw, unvalidated query-string values straight into `.filter()`, so a malformed `individualId`/`dataAgreementId`/`revisionId` raises an uncaught `ValueError` (or Django's non-DRF `ValidationError`) that escapes as an HTTP 500 instead of the 400 the BB's own helper functions exist to produce.
- **MEDIUM** — The signature endpoints only accept a CivicOS-specific nested `{"signature": {...}}` envelope; a spec-conformant **flat** Signature object (which the spec requires, since `signature` is itself a required property name in the schema) is rejected outright as "Expected a dictionary, but got str."
- **MEDIUM** — Right-to-be-forgotten deletes the `ConsentRecord`/`ConsentSignature` rows but leaves the citizen's user ID re-identifiable inside undeletable `ConsentRevision` snapshots (append-only by model-level design) with no anonymization pass — and because the PIPEDA export only queries revisions belonging to *still-existing* records, this residual PII becomes permanently inaccessible to the citizen (a s.4.9 access-right gap) as well as undeletable.
- **MEDIUM** — `POST /config/data-agreement/` accepts and stores DataAgreements with blank `name_en`/`name_fr`/`purpose_fr` — no server-side requirement for the bilingual, human-readable fields the model's own PIPEDA rationale depends on.
- **LOW** — Data-consumer verification endpoints return every citizen's signed consent records with no per-consumer DataAgreement scoping (matches the spec's literal permission model, but is a meaningful blast-radius fact for the security self-assessment); `ConsentRecordGovStackSerializer.state` is technically writable in the serializer definition though not currently reachable; webhook secrets are echoed in plaintext on every read (spec-mandated, but worth flagging) with no replay-window protection beyond HMAC signing.

### Verification detail
- **Test suite:** `python manage.py test apps.consent -v 2` → **296 run / 295 passed / 0 failed / 0 errors / 1 skipped** (the single skip is exactly the documented SQLite concurrency test — nothing else skipped or erroring).
- **Migrations:** `makemigrations --check --dry-run consent` → `No changes detected in app 'consent'`.
- **Harness coverage reality check:** the upstream `GovStackWorkingGroup/bb-consent` repo's `test/gherkin/features/` contains exactly `smoke.feature` and `data_agreement.feature` (plus an unused example-steps stub) — there is no larger untested harness being skipped. The session's harness run therefore covered 100% of what upstream actually ships, but that upstream harness itself only exercises 2 unauthenticated GET operations out of roughly 40 in the full OpenAPI spec. All 5 defects above sit in the ~95% of the surface the harness never touches (Individual CRUD, signature create/update, webhook dispatch, malformed-filter handling).

### Minimum fix list to reach READY
1. Fix the Individual detail path to parse an integer PK (or add a UUID-compatible identifier layer), and correct the false `uuid` claim in `govstack_urls.py`'s docstring.
2. Make `attach_signature()` actually persist the caller's `signature`/`verification_payload_hash`/`verification_signed_by`, not server-recomputed values.
3. Make signature `PUT` create a new `ConsentRevision` and an audit entry, exactly like every other consent-record mutation in the BB.
4. Reuse the Appointments BB's SSRF-safe URL validation (DNS-resolved private-range blocking, `allow_redirects=False`) for `ConsentWebhook.payload_url`.
5. Route the 3 unvalidated filter query params through the existing `_parse_uuid_param`/`_parse_int_param` helpers so malformed input 400s instead of 500ing.
6. Accept the spec's flat Signature envelope in addition to (or instead of) the nested CivicOS-specific one.
7. Add an anonymization pass for RTBF'd citizens' revision snapshots, or explicitly document the retention as a declared, access-right-compliant exception.

---

## Payments BB

*(Fix pass completed and independently re-verified after the audit below. Verdict: ✅ READY. This BB was not part of the original 3-BB scope of this report — it was added because it is the 4th and final GovStack Building Block in this codebase, and the same certifiability bar applied to the other three has now been applied to it too. Original NOT-READY audit findings retained for the record, each annotated with its fix.)*

### Executive verdict — UPDATED
✅ **READY.** All 8 findings from the fresh adversarial audit below (1 CRITICAL, 3 HIGH, 2 MEDIUM, 1 MEDIUM/needs-confirmation, 1 LOW group) have been fixed, using 3 parallel fix agents split by non-overlapping file/function ownership (P2G service + view rewrite for tenant scoping and spec conformance; SSRF hardening + throttle + settings + async-dispatch investigation; docs refresh + independent missed-issue sweep), followed by personal line-by-line diff review of every changed file, a full Payments test suite run, a migration-drift check, and a full cross-BB regression run (Documents, Appointments, Consent) confirming zero destabilization. Personal verification also surfaced and fixed two things the fix agents themselves did not fully close: a genuine, previously-undiagnosed **platform-wide** Celery bootstrap defect (`config/__init__.py` was completely empty, so every `@shared_task` across the entire platform — not just Payments — silently bound to Celery's own unconfigured default app rather than this project's Django-configured one), and a real, self-discovered gap where a sibling agent's documentation update claimed a regression test existed for that fix when it did not — closed by personally writing that test.

### Executive verdict (original, at time of audit)
**NOT READY.** Payments has by far the deepest prior remediation history of any BB in this repo (9 build-out waves, then rounds internally referred to as GAP-1 through GAP-9, then P0–P3, then "§24 Issues A/B/C" plus two secondary findings, then a final SSRF/400-validation/migration/serializer-defect commit `4c4abb8`) — and that history is genuinely real; several of its claims independently re-verified true in this pass (see below). But the P2G (bill payment) surface was never brought to the same bar as G2P/Voucher: it has a CRITICAL cross-tenant data-isolation gap that also allows unauthorized write access, plus three still-open HIGH spec-conformance mismatches against the live OpenAPI spec, and its SSRF protection — while real — is a materially weaker, stale copy of the exact pattern already hardened in Appointments and Consent this same session.

### Method
3 parallel read-only agents (no code edits), each with independent live-spec fetches (not from memory): Agent 1 covered the model/service/task layer against a fresh clone of `GovStackWorkingGroup/bb-payments`'s P2G YAMLs and the live voucher-status-check Cucumber harness; Agent 2 covered the views/API/auth/security layer against the same freshly-fetched specs, actively probing for auth bypass and IDOR; Agent 3 executed the real test suite, a migration-drift check, cross-BB regression, and — going further than any prior round of this project — **actually cloned and ran the live upstream Cucumber/`test/openAPI` harness against a real running instance of CivicOS** via a throwaway reverse-proxy bridging the harness's hardcoded `localhost:3333` to CivicOS's real `/govstack/payments/` mount.

### Confirmed genuinely fixed (from the prior `4c4abb8` commit and earlier rounds)
- **Voucher status-check malformed-input 400.** Independently re-verified against the actual live Cucumber feature file (`voucher_status_check.feature`) and its step definitions: `voucherserialnumer = "{}"` → 400, `= "notAnumber"` → 456 — both scenarios match the current code exactly.
- **Migration drift.** `makemigrations --check --dry-run payments` → clean. The previously-flagged `AlterField` drift was genuinely resolved by migration `0024_alter_bulkpaymentbatch_request_id_and_more.py`.
- **The unauthenticated-caller-reaches-SSRF-sink auth chain (G2P side).** All 5 G2P views now correctly require `IsTrustedSourceBB` instead of `AllowAnyBB`, and `GOVSTACK_REQUIRE_REGISTERED_BB` has a safe explicit default in both `base.py` (False) and `production.py` (True) — an anonymous caller can no longer reach the bulk-payment/prepayment code path that eventually calls the callback-dispatch SSRF sink.
- **Audit trail immutability, voucher state machine, bulk-payment/prepayment idempotency, admin hardening.** All independently confirmed solid — no findings.
- **Test suite and cross-BB regression.** 1709/1709 Payments tests pass; Documents (1151/1151), Appointments (848/848), and Consent (361/361, 1 expected skip) all remain green against current HEAD — Payments' prior work has not destabilized any of the three now-READY BBs.

### Original findings — fix status

- **CRITICAL — P2G endpoints had no tenant/ownership data scoping, permitting cross-tenant reads AND unauthorized bill mutation.**
  ✅ FIXED. Added `platform_tenant_id` to `GovStackBill` (it previously had no tenant field at all; `GovStackBillPayment` already had one) via migration `0025_govstackbill_platform_tenant_id.py`. All 4 `GovStackP2GService` methods (`get_bill`, `create_transfer_request`, `mark_bill_paid`, `get_transfer_request`) now accept `platform_tenant_id` and scope every query by it when non-empty — mirroring the existing `_validate_platform_tenant_id()` mode-gating (harness/test mode permissive, `GOVSTACK_REQUIRE_PLATFORM_TENANT_ID=True` mandatory in production). A wrong-tenant lookup and a genuinely-missing record now raise the identical `BillNotFound`/`BillPaymentNotFound` — never distinguishable from outside, closing the enumeration side-channel too. `mark_bill_paid` also now records the real caller's identity (`actor_payer_fi_id`) as `actor_bb_id` on the audit entry, replacing a hardcoded empty string. New tenant-scoping tests (cross-tenant 404, matching-tenant success, harness-mode-no-tenant success) added to `test_govstack_p2g.py` and `test_govstack_services.py`.

- **HIGH — Payments' SSRF callback guard was a weaker, stale copy of the pattern already hardened in Appointments/Consent.**
  ✅ FIXED. `_is_safe_callback_url()` in `govstack_tasks.py` rewritten to match the Appointments/Consent standard exactly: HTTPS-only (plain `http://` no longer accepted), fails closed on DNS-resolution failure (previously failed open), and now blocks the RFC 6598 CGNAT range (`100.64.0.0/10`) and the IANA IETF Protocol Assignments block (`192.0.0.0/24`) in addition to the private/loopback/link-local/reserved/multicast/unspecified ranges already checked. `_post_callback()` now explicitly treats any `3xx` response as a failed delivery before `raise_for_status()`, rather than silently logging a followed-but-not-really redirect as success. This is now the third identical, independently-verified copy of this mitigation in the codebase (Appointments, Consent, Payments). New tests: `test_g21_post_callback_treats_3xx_as_failure_not_delivered`; existing G17–G19 unsafe-URL fixtures updated from `http://` to `https://` since HTTPS-only is now enforced.

- **HIGH — P2G response envelope and status codes didn't match the live spec.**
  ✅ FIXED. Re-fetched and read the live GovStack `bb-payments` OpenAPI YAMLs directly (`billInquiryResponse.yml`, `billPaymentRequest.yml`, `rtpStatusUpdateRequest.yml`, `markBillPpaymentRequest.yml`) rather than relying on paraphrase. All 4 P2G views now return **HTTP 202** with the spec-required `{responseCode (maxLength 2), reason, requestID (maxLength 12)}` envelope, alongside (not instead of) the existing useful data fields (`billId`, `amount`, `currency`, etc.) — no schema in the spec sets `additionalProperties: false`, so both are valid simultaneously. **Deliberate, documented deviation:** P2G stays synchronous rather than implementing the async-callback pattern the response schema's shape implies, because no P2G request YAML has a callback-URL registration field and no P2G harness exists upstream to validate against — recorded explicitly in `SPEC_GOVSTACK_PAYMENTS_BB.md` §26 as a reasoned choice, not an oversight.

- **HIGH — `GET /transferRequests/{id}` was wired to the wrong header family.**
  ✅ FIXED. Confirmed via the live `rtpStatusUpdateRequest.yml` that the spec requires `X-billerId` with no PayerFI header anywhere in that file. Added a new `IsTrustedBiller` permission class (`header_names = ("X-billerId", "X-BillerId", "X-billerID")`) and switched `TransferRequestStatusView.permission_classes` from `IsTrustedPayerFI` to it.

- **HIGH — `GET /bills/{billId}` didn't implement the spec-mandated `fields=inquiry` query parameter.**
  ✅ FIXED. `BillInquiryView` now reads and requires the `fields=inquiry` query parameter per the live spec, returning a clean 400 when absent or wrong.

- **MEDIUM — No BB-identity-keyed throttle for Payments.**
  ✅ FIXED. New `GovStackPaymentsIdentityThrottle(ScopedRateThrottle)` (`apps/payments/govstack_throttling.py`) keys the rate-limit bucket on the calling BB's resolved identity (stashed as `request.META["_gs_payer_identity"]` by `_HeaderWhitelistBBPermission.has_permission()`) instead of client IP, falling back to stock IP-based behaviour when no identity is present — same pattern as Appointments'/Consent's throttles. Wired in as `GovStackAPIView.throttle_classes`. New `test_govstack_throttling.py` (6 tests) mirrors Appointments' throttle test structure.

- **MEDIUM — Inconsistent application of the `base.py` safe-default settings convention.**
  ✅ FIXED. `GOVSTACK_REQUIRE_REGISTERED_PAYER_FI`, `GOVSTACK_REQUIRE_PLATFORM_TENANT_ID`, `GOVSTACK_VOUCHER_REQUIRE_JWT`, and `GOVSTACK_VOUCHER_REQUIRE_REGISTERED_BB` are now all defined explicitly in `base.py` with `env.bool(..., default=False)`, matching the `GOVSTACK_SCHEDULER_REQUIRE_TOKEN`/`GOVSTACK_REQUIRE_REGISTERED_BB` convention established this session. `production.py`'s existing `default=True` overrides are untouched.

- **MEDIUM/needs-confirmation — a real-server async-dispatch defect found live-harness-side.**
  ✅ CONFIRMED AND FIXED — root cause was **platform-wide, not Payments-specific**. Investigation traced the live-harness `Connection refused` failures to `config/__init__.py` being completely empty: it never imported `from .celery import app as celery_app`, so every `@shared_task` across the *entire platform* lazily bound to Celery's own unconfigured default app (real AMQP broker, `task_always_eager=False`) on first `.delay()` call, rather than this project's Django-settings-configured app — invisible to the test suite because nearly every test mocks `.delay()` out. Fixed by adding the missing import to `config/__init__.py`. Directly verified via reproduction: a real, unmocked `.delay()` call raised `kombu.exceptions.OperationalError: Connection refused` before the fix and executed synchronously after. **New regression coverage added personally** (a sibling agent's doc update had claimed this test already existed; it didn't) — `CeleryAppBootstrapTest` in `apps/payments/tests/test_apps_config.py` (4 tests), including a real unmocked `.delay()` call that would now catch any future regression of this import. Fixing this platform-wide bug also surfaced one genuine, narrow regression in a *different*, already-certified BB: a Consent BB test (`test_failure_sets_status_failed`) assumed eager-mode `self.retry()` would respect `throw=False`, which is not true — Celery's `Retry` exception always propagates out of `.apply()` in eager mode regardless of `throw`, a fact the old (broken) bootstrap had been accidentally masking. The application code was never wrong (it already set `status=STATUS_FAILED` before the retry); only the test's assumption was — fixed by catching `Retry` explicitly around the `.apply(throw=False)` call, with a comment explaining why.

- **LOW/cosmetic group** — stale masthead, stale test docstrings, unchecked Definition-of-Done boxes, unfilled gap ledger.
  ✅ FIXED. `SPEC_GOVSTACK_PAYMENTS_BB.md` masthead updated (test count, harness-execution status); ToC fixed; Wave 5 "Definition of Done" checkboxes checked with detailed notes; new §26 "Round 4 Remediation" section documents the full audit-to-fix trail for future maintainers. Stale `test_govstack_vouchers.py` docstrings (D4/D8) corrected from "HTTP 400" to "HTTP 456" (test bodies were already correct — only the docstrings lied). Header-based BB authentication (`_HeaderWhitelistBBPermission`) remains a bare string/whitelist match with no cryptographic binding, per the GovStack spec's own `apiKey in: header` design — not code-changed, since it's spec-mandated, but now much lower-risk given the CRITICAL tenant-scoping fix above closes the practical exploit path that made this matter.

### Verification detail — UPDATED
- **Test suite:** `python manage.py test apps.payments -v 1` → **1750/1750 pass, 0 failures, 0 errors** (1709 original + 41 new/updated across the 3 fix agents' test files plus the personally-added `CeleryAppBootstrapTest` class).
- **Migrations:** `makemigrations --check --dry-run payments` → `No changes detected in app 'payments'` (the new `platform_tenant_id` field on `GovStackBill` is captured cleanly by migration `0025`).
- **Cross-BB regression check:** `apps.consent` → **361/361 pass, 1 expected skip** (after fixing the one genuine regression described above — a test assumption, not application behavior); `apps.appointments` → **848/848 pass**; `apps.documents apps.api.documents` → **1151/1151 pass** — confirms the platform-wide `config/__init__.py` fix and the Payments-scoped changes destabilized nothing else across the other three certified BBs, once the one real test-assumption fix was applied.
- **Manual review of every agent's diff:** personally read the full diff of every changed file (`govstack_services.py`'s tenant-scoping rewrite across all 4 P2G methods, `govstack_views.py`'s envelope/status-code/header/query-param rewrite across all 4 views, `govstack_tasks.py`'s SSRF hardening, `govstack_auth.py`'s new `IsTrustedBiller` class and identity-stashing, `govstack_throttling.py`, the `config/__init__.py`/`config/settings/base.py` changes, and every test file) — all confirmed correct, consistent with sibling-BB conventions, and properly commented with rationale for every judgment call (the synchronous-P2G deviation, the actor-identity audit fix, the platform-wide Celery bootstrap fix and its one downstream test fix). Personal verification also caught and closed two things the fix agents alone did not: the platform-wide Celery bootstrap bug's true blast radius (initially scoped to Payments by the fix agent, actually platform-wide), and a doc claim of a regression test that didn't yet exist (now written and passing).

### Original findings (severity-ranked, as of the pre-fix audit)

- **CRITICAL — P2G endpoints have no tenant/ownership data scoping, permitting cross-tenant reads AND unauthorized bill mutation.** `GovStackP2GService.get_bill()`, `get_transfer_request()`, and `mark_bill_paid()` all query by bare `bill_id`/`request_id` with no filter on `payer_fi_id` or `platform_tenant_id`. `X-Platform-TenantId` is validated for presence/length but the view layer's own docstrings admit the validated value is "not persisted anywhere" and never used to scope a query. Practical impact: any caller holding a valid, whitelisted `X-PayerFI-Id` (a bare string/whitelist match with no cryptographic binding) can `GET` another institution's bill or transfer-request, and — the more severe half of this finding — can `POST /bills/{bill_id}/mark-paid` on **any tenant's bill**, which creates no `GovStackBillPayment` audit record at all, making it a clean way to make a bill vanish from accounts-receivable with zero trace. The live P2G YAML specs explicitly document `Platform-TenantId`/`X-Platform-TenantId` as `required: true` with the description "Tenant Id for Data scoping" — this was a direct, confirmed contradiction of the header's documented spec purpose, not a defensible design choice.
- **HIGH — Payments' SSRF callback guard is a weaker, stale copy of the pattern already hardened in Appointments/Consent this session.** `_is_safe_callback_url()`/`_post_callback()` in `govstack_tasks.py` still allows plain `http://` (not HTTPS-only), does not block RFC 6598 CGNAT (`100.64.0.0/10`) or the IANA IETF Protocol Assignments block (`192.0.0.0/24`) that the other two BBs' identical-purpose functions explicitly add, and never checks for a 3xx response after `allow_redirects=False` (so a redirect response is logged as a successful delivery even though it wasn't followed) — an inconsistency the same commit wave demonstrably knew how to avoid, since it did so correctly in two sibling BBs.
- **HIGH — P2G response envelope and status codes still don't match the live spec, confirmed unfixed despite 3 subsequent commits.** The live spec requires HTTP 202 with `{responseCode, reason, requestID}` for `GET /bills/{billId}`, `POST /billTransferRequests`, `GET /transferRequests/{id}`, and `POST /bills/{id}/mark-paid`; CivicOS returns HTTP 200 with bespoke response bodies on all four. Because no P2G Cucumber harness exists upstream (confirmed — only G2P and Voucher `.feature` files exist), the raw OpenAPI YAML is the *only* certification artifact for P2G, making this a genuine, uncovered gap rather than a defensible harness-vs-spec judgment call.
- **HIGH — `GET /transferRequests/{id}` is wired to the wrong header family.** The live spec (`rtpStatusUpdateRequest.yml`) requires `X-billerId` with no PayerFI header anywhere in that file; the view uses `IsTrustedPayerFI` (`X-PayerFI-Id` family) instead. `X-billerId` is not read anywhere in the codebase.
- **HIGH — `GET /bills/{billId}` does not implement the spec-mandated `fields=inquiry` query parameter**, which the live spec marks `required: true`. The view never reads `request.query_params` at all.
- **MEDIUM — No BB-identity-keyed throttle for Payments**, unlike the dedicated throttle classes added to Appointments and Consent this session. All 13 Payments endpoints share one generic IP-keyed `ScopedRateThrottle` bucket, with no test coverage of the throttle path (test settings raise the rate to effectively unlimited).
- **MEDIUM — Inconsistent application of the `base.py` safe-default convention established this session.** `GOVSTACK_REQUIRE_REGISTERED_PAYER_FI`, `GOVSTACK_REQUIRE_PLATFORM_TENANT_ID`, `GOVSTACK_VOUCHER_REQUIRE_JWT`, and `GOVSTACK_VOUCHER_REQUIRE_REGISTERED_BB` are defined only in `production.py` (safe `default=True` there) and absent from `base.py` entirely — currently harmless only because each flag's own `getattr(..., False)` fallback happens to coincide with the secure default, but a latent trap against the exact fail-open pattern already fixed for 3 other flags across the other BBs this session.
- **MEDIUM/needs-confirmation — a real-server async-dispatch defect was found live-harness-side, invisible to the test suite.** Running the actual live harness against a real running instance surfaced 4 scenario failures traced to `BulkPaymentView`/`PrepaymentValidationView`'s `transaction.on_commit(lambda: ...delay())` attempting a genuine AMQP broker publish and failing with `Connection refused`, even though `CELERY_TASK_ALWAYS_EAGER` should make `.delay()` execute synchronously with no broker involved. The only tests that exercise this code path mock `.delay()` out entirely, so this path has literally never been tested end-to-end by `manage.py test`. This may be an artifact of the audit's own throwaway settings override rather than a genuine production defect (i.e., it needs to be reproduced against `config.settings.test` directly, not a custom override, before being treated as fully confirmed) — flagged here as a real coverage gap regardless of root cause: no test in 1709 exercises the real, unmocked `.delay()` → task-execution path.
- **LOW/cosmetic group** — `SPEC_GOVSTACK_PAYMENTS_BB.md`'s own masthead is stale (claims "1,633 tests" and "not yet run against the live harness"; both now false — 1709 tests and the harness has genuinely been run); a stale test-docstring in `test_govstack_vouchers.py` still describes 2 tests as expecting HTTP 400 when their bodies correctly assert 456; §14's Wave 5/P2G "Definition of Done" checkboxes are still unchecked despite Wave 5 being implemented, and this staleness was never carried into the doc's own §22–25 gap ledger — a real instance of the "no living gap-ledger forces re-confirmation" problem this project has flagged before, this time inside a doc that otherwise exists specifically to prevent it. Header-based BB authentication (`_HeaderWhitelistBBPermission`) is a bare string/whitelist match with no cryptographic binding — this is the GovStack spec's own `apiKey in: header` design, not a CivicOS-introduced bug, but it directly compounds the CRITICAL finding above (once a valid identifier is known, full cross-tenant access follows with nothing else to check).

### Verification detail (original, at time of audit)
- **Test suite:** `python manage.py test apps.payments -v 1` → **1709/1709 pass, 0 failures, 0 errors.**
- **Migrations:** `makemigrations --check --dry-run payments` → `No changes detected in app 'payments'`.
- **Live harness (actually executed, not just read about):** cloned `GovStackWorkingGroup/bb-payments`'s real `test/openAPI` Cucumber suite, bridged its hardcoded `localhost:3333` base URL to CivicOS's real mount via a throwaway reverse proxy, and ran all 9 feature files twice with byte-identical results: **74 scenarios: 65 passed, 9 failed** (606 steps: 550 passed, 47 skipped, 9 failed). Of the 9 failures: 5 are a harness-side duplicate-request artifact (independently confirmed via Django's raw request log showing each PATCH logged twice per scenario — not a CivicOS defect); 4 are the real async-dispatch coverage gap described above.
- **Cross-BB regression:** Documents 1151/1151, Appointments 848/848, Consent 361/361 (1 expected skip) — all green against current HEAD.
- **Scope confirmation:** `apps/api/{notifications,portal,volunteers,workflows}` grepped for any GovStack/upstream-spec/harness reference — zero hits beyond CivicOS's own generic "building block" architecture terminology. Payments is confirmed the 4th and final GovStack BB in this repo.

---

## Cross-cutting observations

1. **The "green suite, spec-blind" pattern is now confirmed across all three BBs**, not just Documents (where it was first found this session). Every BB's tests were written against its own implementation rather than against the live upstream spec/Gherkin, so status-code, envelope-shape, and identifier-type mismatches with the real spec are systematically invisible to `manage.py test`. Recommendation: for any BB claimed "done," a live-harness or hand-written spec-literal-request test is not optional polish — it is the only thing that actually catches this class of bug, and this audit again proves it every time it's applied.
2. **SSRF protection is inconsistent across BBs that share the exact same risk** (an operator/citizen-supplied callback URL dispatched later by a Celery task). Appointments/Scheduler has a strong, DNS-resolving, redirect-blocking implementation. Consent has none on its webhook path. This should be extracted into one shared utility and applied uniformly rather than re-invented per BB.
3. **Settings-module fail-open/fail-closed asymmetry recurs 3 times** (Documents' `CLAMAV_REQUIRED`, Appointments' `GOVSTACK_SCHEDULER_REQUIRE_TOKEN`, Consent's `GOVSTACK_REQUIRE_CONSENT_AUTH`): all three security-mode flags are defined only in `production.py` with a safe default there, but every non-production settings module (`base.py`/`development.py`/`test.py`) simply omits them, and the `getattr(..., default=...)` fallback used at each call site doesn't consistently resolve to the secure behavior in that absence. Recommend defining all three flags in `base.py` with the secure (`True`/required) default, and letting `production.py` simply inherit rather than re-declare — so "the setting doesn't exist" and "the setting is off" can never be confused again.
4. **Identifier-type mismatches (int vs. UUID vs. prefixed string) are a recurring class of bug**, not a one-off: Documents' serializer misdeclaring an integer PK as `UUIDField` (cosmetic), Appointments' `R-<pk>` canonical form breaking at the Affiliation-endpoint boundary, and Consent's Individual-detail 400s are three independent instances of the same root cause — a per-BB assumption about an ID's type that isn't verified against the actual model field.
5. **Audit-trail completeness gaps appear in exactly the two places that matter most for a government compliance product**: Documents' missing malware-detection audit entry, and Consent's missing signature-mutation audit entry. Both BBs otherwise have thorough, well-designed audit logging elsewhere — these look like isolated omissions rather than a systemic gap, but both are squarely inside PIPEDA/security-logging expectations for a certifiable public-sector BB.

## Overall verdict

**UPDATE 3 (Payments BB fix pass completed — all 4 GovStack BBs now READY):** The Payments BB fix pass has been completed and independently re-verified using the same disciplined method as the other three BBs: 3 parallel fix agents given strictly non-overlapping ownership (P2G service+view rewrite; SSRF/throttle/settings/async-dispatch; docs+missed-issue sweep), personal line-by-line diff review, a full 1750/1750-passing test suite run, a clean migration-drift check, and a full cross-BB regression run (Consent 361/361, Appointments 848/848, Documents 1151/1151) confirming zero destabilization. All 8 original findings (1 CRITICAL P2G tenant-isolation gap, 3 HIGH spec-conformance/SSRF gaps, 2 MEDIUM, 1 MEDIUM/needs-confirmation, 1 LOW group) are closed — see the updated "Payments BB" section above for the full per-finding fix log. Personal verification surfaced one significant fact beyond the fix agents' own reports: the async-dispatch defect one agent had scoped to Payments was actually a **platform-wide** bug (`config/__init__.py` never importing the Celery app), fixed at the platform level, with its one real downstream side-effect — a Consent BB test whose eager-mode-retry assumption was wrong — found and fixed in the same pass. **All four GovStack Building Blocks in this repo — Documents Management, Appointments/Scheduler, Consent, and Payments — are now ✅ READY for GovStack certification submission.**

**UPDATE 2 (Payments BB discovered and audited):** A 4th GovStack Building Block — Payments — was confirmed to exist in this codebase and was put through the same fresh, adversarial, 3-agent audit as the other three, surfacing the findings fixed in UPDATE 3 above.

**UPDATE 1 (post-fix-pass, 3-BB scope):** The original 3-BB scope of this report — Documents Management, Appointments/Scheduler, and Consent — reached ✅ **READY** using the same disciplined method each time: 3 parallel fix agents given strictly non-overlapping file/function ownership, followed by personal line-by-line diff review (not just aggregate test-pass counts), a full BB-specific test suite run, a `makemigrations --check --dry-run` drift check, and a full cross-BB regression run confirming zero destabilization of previously certified work. Every judgment call made by a fix agent (e.g. Consent's reuse of the `"granted"` audit action for signature updates pending a dedicated choice value, the accepted single-tenant `GovStackRegisteredBB.role` scoping in Appointments, Payments' deliberate synchronous-P2G deviation) is explicitly documented in-line as a deliberate, narrowly-scoped decision rather than a silent shortcut, so a future maintainer can find and revisit it without re-deriving the reasoning from scratch.

**Original verdict (at time of the initial audit, for the record):** None of the three BBs closed out this session were ready for GovStack certification submission as-is. All three, however, were close: every blocking defect identified had an existing, correct pattern already implemented elsewhere in the same codebase (or in a sibling BB) that the fix could mirror. Recommended order of attack, by blast radius and fix simplicity, was: (1) Documents' one-line PDF-encryption inversion — trivial fix, currently breaks the single most common document type; (2) Consent's Individual-detail UUID/int mismatch — breaks 5 spec operations outright; (3) Appointments' array-filter and create-status-code gaps — mechanical, the correct pattern already exists in the same file for 2 of 8 fields; (4) the two signature-integrity findings in Consent and the SSRF gap in Consent's webhooks — these touch the compliance/security posture most directly and should not ship un-remediated even if a harness run would technically pass without them. This order was, in effect, exactly the order followed across the three fix passes that produced the updated verdict above.
