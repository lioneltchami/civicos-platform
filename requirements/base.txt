# Generated from base.in — target: Python 3.12, Django 5.2 LTS
# Regenerate with: python -m piptools compile requirements/base.in -o requirements/base.txt

# Core framework
Django==5.2.3
wagtail==6.4.1

# Database
psycopg[binary]==3.2.3

# Environment configuration
django-environ==0.11.2

# Authentication
django-allauth==65.3.0
django-otp==1.5.4
django-two-factor-auth==1.16.0
qrcode[pil]==7.4.2
phonenumbers==8.13.52

# Password hashing
argon2-cffi==23.1.0
argon2-cffi-bindings==21.2.0

# Security
django-csp==3.8
django-ratelimit==4.1.0
django-ipware==7.0.1

# File storage
django-storages[s3]==1.14.4
Pillow==11.0.0
boto3==1.35.68

# Email / notifications
django-anymail==12.0
notifications-python-client==9.0.0

# Task queue
celery[redis]==5.4.0
redis==5.2.1
django-celery-beat==2.8.0
django-celery-results==2.5.1
kombu==5.4.2
billiard==4.2.1
vine==5.1.0

# Static files
whitenoise[brotli]==6.8.2
brotli==1.1.0

# Export
openpyxl==3.1.5
et-xmlfile==2.0.0

# Payments
stripe>=15.0.0,<16  # M9: pinned to installed major (15.x); Stripe has breaking changes across majors

# PDF generation — CRA donation receipts
weasyprint>=60.0,<70  # M10: upper bound added; installed is 69.x; WeasyPrint has breaking API changes across majors

# REST API
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
drf-spectacular>=0.27,<1.0

# Utilities
python-slugify==8.0.4
text-unidecode==1.3

# Indirect dependencies (pinned for reproducibility)
asgiref==3.8.1
sqlparse==0.5.2
# cryptography>=44.0.0 — fixes CVE-2024-26130 (PKCS12 null deref) and
# CVE-2024-12797 (RSA key timing). Lower bound was 43.0.3.
cryptography>=44.0.0,<46
cffi==1.17.1
pycparser==2.22
