"""
Volunteer Management BB — URL configuration.

Wave 1: Empty stub — all URL patterns implemented in Wave 2 (portal views)
and Wave 3 (scheduling/hours views).
"""
from django.urls import path

app_name = "volunteers"

urlpatterns = [
    # Wave 2+: portal, coordinator, and admin views wired here.
]
