"""
Payment BB signal receivers.

Registered in PaymentsConfig.ready() via:
    import apps.payments.receivers  # noqa: F401

Implementations added in Wave 1 Round 2 (gateway integration + webhook processing).
"""
